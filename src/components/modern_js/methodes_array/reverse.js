const reverse_getLetters = () => {
  return ["A", "B", "C", "D", "E"];
};

const reverse_getLetters_test = () => {
  return reverse_getLetters().reverse();
};

export { reverse_getLetters, reverse_getLetters_test };
